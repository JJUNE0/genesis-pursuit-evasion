"""RL agents package.

Phase 1 ships only ``agents.ppo_runner`` (rsl-rl 5.2 thin wrapper for single
runner training).  Forecaster (Phase 3), critic obs builder (Phase 4), and
AMS-DRL orchestrator (Phase 5) live in their own subpackages and are not
imported eagerly here.
"""
