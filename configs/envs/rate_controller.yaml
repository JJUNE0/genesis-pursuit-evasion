# Auto-generated by scripts/tune_rate_pid.py
# 단계 2 — body-rate PD gain tuning result.
# 적용: configs/envs/pe_1v1_default.yaml의 action.rate_controller 블록에 복사
#       또는 직접 read 후 env_cfg에 주입.
rate_controller:
  kp: [0.05, 0.1, 0.1]   # roll, pitch, yaw
  kd: [0.0001, 5e-05, 0.0001]
  max_rate: 3.14159   # ±π rad/s
  max_thrust: 15.7209   # = 2 × m·g  (0.8013 × 9.81)
